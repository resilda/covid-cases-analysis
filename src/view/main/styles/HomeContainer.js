import styled from 'styled-components'

export const HomeContainer = styled.div`
  & .welcome-wrapper {
    text-align: center;
  }

  & .welcome-title {
    font-size: 30px;
    margin-top: 60px;
  }

  & .welcome-rubrics {
    font-size: 17px;
    margin-bottom: 60px;
  }
`
